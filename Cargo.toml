[package]
name = "poker_gto_engine"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
wide = "0.7"
flate2 = "1.0"

[profile.release]
lto = "fat"
codegen-units = 1
opt-level = 3
